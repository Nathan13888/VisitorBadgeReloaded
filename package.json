{
  "name": "vbr",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types",
    "bench": "bun run benchmark/benchmark.ts",
    "bench:cache": "bun run benchmark/cache-benchmark.ts",
    "bench:usage": "bun run benchmark/usage-simulation.ts",
    "bench:latency": "bun run benchmark/latency-test.ts",
    "bench:compare": "bun run benchmark/compare.ts",
    "bench:report": "bun run benchmark/generate-report.ts",
    "bench:all": "bun run benchmark/benchmark.ts && bun run benchmark/cache-benchmark.ts && bun run benchmark/latency-test.ts && bun run benchmark/generate-report.ts"
  },
  "dependencies": {
    "@hono/standard-validator": "^0.1.5",
    "@scalar/hono-api-reference": "^0.9.22",
    "@scalar/openapi-to-markdown": "^0.2.45",
    "hono": "^4.6.11",
    "hono-openapi": "^1.1.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@cloudflare/workers-types": "^4.20241022.0",
    "@types/bun": "^1.3.0",
    "@types/node": "^24.8.1",
    "wrangler": "4"
  }
}
